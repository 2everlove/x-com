import { ReactNode } from "react";

export default function AfrterLogin({children}: {children: ReactNode }) {
    return (
        <div>
            AfrterLogin
            {children}
        </div>
    )
}